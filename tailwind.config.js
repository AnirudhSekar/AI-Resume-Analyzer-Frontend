/** @type {import('tailwindcss').Config} */
export default {
    content: ["./src/**/*.{js,jsx}"], // Ensures Tailwind scans JS & JSX files in `src/`
    theme: {
      extend: {}, // Customize Tailwind styles here
    },
    plugins: [],
  };
  